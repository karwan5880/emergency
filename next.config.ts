import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack handles module resolution automatically
  // The path alias in tsconfig.json is sufficient
};

export default nextConfig;
